---
import { Download, CheckCircle2 } from "lucide-astro";

const benefits = [
  "Instalación en menos de 5 minutos",
  "Completamente gratuito",
  "Sin necesidad de registro",
  "Actualizaciones automáticas",
];
---

<section class="relative py-24">
  <div class="absolute inset-0 -z-10">
    <div
      class="absolute top-1/2 left-1/2 h-96 w-96 -translate-x-1/2 -translate-y-1/2 rounded-full bg-blue-500/10 blur-3xl"
    >
    </div>
  </div>

  <div class="container mx-auto px-6">
    <div
      class="relative overflow-hidden rounded-2xl border border-gray-200 bg-gradient-to-br from-white to-gray-50 p-12 shadow-xl backdrop-blur-sm md:p-16"
    >
      <div
        class="absolute top-0 right-0 h-64 w-64 rounded-full bg-blue-500/5 blur-3xl"
      >
      </div>
      <div
        class="absolute bottom-0 left-0 h-64 w-64 rounded-full bg-teal-500/5 blur-3xl"
      >
      </div>

      <div class="relative mx-auto max-w-3xl text-center">
        <h2
          class="mb-6 text-balance text-4xl font-bold tracking-tight text-gray-900 md:text-5xl"
        >
          Descarga InventoryPro y comienza hoy mismo
        </h2>
        <p class="mb-8 text-pretty text-lg text-gray-600">
          Únete a cientos de empresas que ya confían en InventoryPro para
          gestionar sus productos y proveedores de forma eficiente. Descarga
          gratuita, sin registro.
        </p>

        <div class="mb-10 flex flex-wrap items-center justify-center gap-6">
          {
            benefits.map((benefit, index) => (
              <div class="flex items-center gap-2 text-sm text-gray-700">
                <CheckCircle2 class="h-5 w-5 text-blue-600" />
                <span>{benefit}</span>
              </div>
            ))
          }
        </div>

        <div
          class="flex flex-col items-center justify-center gap-4 sm:flex-row"
        >
          <button
            class="cursor-pointer inline-flex items-center justify-center gap-2 rounded-lg bg-blue-600 px-6 py-3 text-base font-medium text-white transition-colors hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2"
          >
            <a href="https://github.com/InventoryPro/InventoryPro/releases/latest"
              target="_blank"
              rel="noopener noreferrer"
              class="flex items-center gap-2"
            >
              <Download class="h-5 w-5" />
              Descargar Instalador (Windows)
            </a>
          </button>
          <button
            class="cursor-pointer inline-flex items-center justify-center rounded-lg border border-gray-300 bg-white px-6 py-3 text-base font-medium text-gray-900 transition-colors hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2"
          >
            <a href="https://github.com/InventoryPro/InventoryPro"
              target="_blank"
              rel="noopener noreferrer"
            >Ver Documentación</a>
          </button>
        </div>

        <p class="mt-6 text-sm text-gray-600">
          Compatible con Windows 10 y superior
        </p>
      </div>
    </div>
  </div>
</section>
